# Life Manager — Frontend Environment Variables
# Copy this file to .env.local for local development

# API base URL — the address of the .NET backend
# Default (local dev): http://localhost:5000
# LAN/production: http://your-server-hostname:5000 or https://api.life.local
VITE_API_URL=http://localhost:5000
